const initialState = [];

const todos = (state = initialState, action) => {
  switch (action.type) {
    case "ADD_TODO":
      return {
        ...state,
        <action className="pay"></action>
      };
    default:
      return state;
  }
};

export default todos;
